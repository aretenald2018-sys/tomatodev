Correct Tomato Farm workout set entry UI: keep the exercise success criteria block and previous-record block, but make the set rows below them match the BodyCalendar reference more closely. A newly added workout exercise must start with only one set row, not four copied rows. The + row should add one new row by copying the previous row values when a previous row exists. Numeric editing should only be available after the user clicks the row set-number/type toggle or the right chevron; the minimal row itself should support completing the set with the check button. Preserve ROM input/editing only inside the expanded editor. Keep vanilla JavaScript, TDS Mobile/Seed style, no www edits, and bump sw.js cache when STATIC_ASSETS change.
