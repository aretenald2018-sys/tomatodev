# 기능 계획 템플릿

> 2026-07-19 갱신: 예전 버전은 계획을 반드시 "실행 슬라이스 1, 2, ..."로 쪼개고 세션마다
> 하나씩만 실행하도록 강제했다. 이제는 그런 강제가 없다 — 아래 "구현 범위"에 한 번에 구현할
> 내용을 자연스러운 만큼(필요하면 순서만 나눠서) 적으면 된다. 이 문서 자체도 계획이 실제로
> 도움이 되는 경우(범위가 모호하거나 여러 모듈에 걸칠 때)에만 쓴다.

## 요청 원문

사용자의 요청을 여기에 붙여넣는다.

## 이해한 내용

- 목표:
- 비목표:
- 사용자 흐름:
- 데이터 가정:
- 열려 있는 질문:

## 그릴 결과 (모호한 설계 질문이 있었을 때만)

- 핵심 질문:
- 추천 답변:
- 사용자 답변:
- 확정된 결정:
- 남은 가정:

## 결정 기록 (되돌리기 비싼 결정이 있었을 때만)

- 결정:
- 이유:
- 되돌릴 수 있는가:

## 구현 범위

- 목표:
- 예상 수정 파일:
- 수정하지 말 것 (이번 범위에서 제외할 것):
- 구현 메모:
- 검증 방법:
- 완료 증거:

필요하면 위 항목을 순서가 있는 여러 단계로 나눠 적어도 되지만, 각 단계를 별도 세션으로
강제하거나 승인 게이트를 두지 않는다 — 안전하게 리뷰 가능한 만큼 한 번에 구현한다.

## 검증/리뷰 메모

이 계획 문서와 변경 파일을 놓고 버그, 회귀, 누락된 테스트, 오래된 캐시/서비스워커 이슈,
UX 깨짐을 점검한다.

## 큰 다중 세션 작업일 때만: NEXT_ACTION.md 메모

- 상태:
- 다음 액션:
- 막힌 지점:
